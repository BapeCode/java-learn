## Exercice 6 : La Moyenne de la classe 🎓
C'est un exercice classique que tout développeur a fait au moins une fois !

**Les consignes :**

1. Dans le ```main```, crée un tableau de ```double``` (pour avoir des nombres à virgule) appelé ```notes```. Mets-y 4 ou 5 notes de ton choix (ex: 10.5, 15.0, etc.).

2. Déclare une variable ```double somme = 0```; (elle servira à accumuler le total des points).

3. Fais une boucle ```for``` qui parcourt tout le tableau (de 0 jusqu'à la fin).

4. À chaque tour de boucle, ajoute la note actuelle à ta variable ```somme``` (Rappel: ```somme = somme + notes[i]```).

5. Une fois la boucle finie, calcule la moyenne (Somme divisée par le nombre de notes). Astuce : utilise ```notes.length``` pour diviser par le bon nombre.

6. Affiche : "La moyenne est de : [ta moyenne]".

C'est un petit challenge logique. Prends ton temps ! Si tu bloques sur l'addition dans la boucle, dis-le-moi.